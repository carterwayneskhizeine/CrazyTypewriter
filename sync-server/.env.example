# Server Configuration
NODE_ENV=development
PORT=3001

# External Auth API URL (for validating sessions)
AUTH_API_URL=https://example.com

# Database Path (relative to project root)
DB_PATH=./data/sync.db

# CORS
CORS_ORIGIN=*
